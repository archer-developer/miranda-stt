# Copy to .env and fill in values. Loaded at startup by internal/envfile.
# Real environment variables always take priority over values in this file.

# Google Gemini API key for the Multimodal Live API (BidiGenerateContent).
# Must match the name given by gemini_api_key_env in config/config.yaml.
GEMINI_API_KEY=
